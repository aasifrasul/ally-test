import hbsTemplate from '../templates/index.hbs';

console.log('template: ==============================', hbsTemplate);
/*
export const helper = {
	if_eq: (a, b, opt) => {
		return opt.fn({});
	},
};
*/
